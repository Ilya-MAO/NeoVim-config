local padding = true -- відступ між знаком коментаря та текстом
local sticky = true -- залишає курсор на місці замість коментування
local ignore = nil -- ігнорувати порожні рядки

return {
	padding = padding,
	sticky = sticky,
	ignore = ignore
}
